# ── Database ──────────────────────────────────────────────────────
# Railway auto-injects DATABASE_URL from the PostgreSQL plugin.
# Locally, use the docker-compose PostgreSQL instance:
DATABASE_URL=postgresql+asyncpg://agent_user:agent_password@localhost:5432/manufacturer_agent
# DATABASE_URL_SYNC is auto-derived if not set explicitly.

# ── Auth ──────────────────────────────────────────────────────────
# REQUIRED: Generate a strong random key for production:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-to-a-random-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ── API Keys ──────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
BRAVE_API_KEY=BSA...

# ── CORS ──────────────────────────────────────────────────────────
# JSON array of allowed frontend origins.
# Production: ["https://your-app.vercel.app"]
CORS_ORIGINS=["http://localhost:3000"]

# ── Environment ───────────────────────────────────────────────────
# Set to "production" on Railway to disable /docs and /redoc.
ENVIRONMENT=development
